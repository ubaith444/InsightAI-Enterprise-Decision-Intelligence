apiVersion: v1
kind: ConfigMap
metadata:
  name: insightai-config
  namespace: insightai
data:
  ENVIRONMENT: production
  API_PREFIX: /api/v1
  CORS_ORIGINS: '["https://insightai.example.com"]'
  POSTGRES_DB: insightai
  MONGO_DATABASE: insightai
