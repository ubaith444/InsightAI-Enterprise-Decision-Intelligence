# Tests

The executable tests are colocated with the applications:

- Backend tests: `backend/tests/`
- Frontend E2E tests: `frontend/e2e/`
- Demo capture tests: `frontend/demo/`

Release validation commands are documented in `docs/TESTING.md`.
